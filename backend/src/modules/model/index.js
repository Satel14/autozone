export default class ModelController {
  static async getList({ brand }) {
    // const errors = []
    // if (!errors.length) return { created: false }

    // try {

    // } catch (e) {
    //   console.error(e)
    //   return { created: false }
    // }

    return { brand }
  }
}
